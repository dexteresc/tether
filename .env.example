# Database (Supabase PostgreSQL)
DB_HOST=db.xxxxxxxxxxxx.supabase.co
DB_USER=postgres
DB_PASSWORD=your-supabase-db-password
DB_NAME=postgres
DB_PORT=5432
DB_SSLMODE=require

# JWT
JWT_SECRET=change-me-to-a-random-string

# CORS — comma-separated allowed origins (your Netlify URL)
CORS_ORIGINS=https://your-site.netlify.app

# LLM (optional)
LLM_PROVIDER=openai
LLM_API_KEY=
LLM_MODEL=
LLM_BASE_URL=
