[build]
  base = "web"
  command = "npm run build"
  publish = "dist"

# SPA redirect — send all routes to index.html for React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
